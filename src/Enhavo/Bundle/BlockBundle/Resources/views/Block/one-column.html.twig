{% if data.width == 'container' %}
    <div class="container">
{% endif %}
    <div class="row">
        <div class="col-md-12">
            {{ block_render(data.column) }}
        </div>
    </div>
{% if data.width == 'container' %}
    </div>
{% endif %}

