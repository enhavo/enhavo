<form action="{{ path('enhavo_contact_submit') }}">
    {{ form_row(form.email) }}
    {{ form_row(form.firstName) }}
    {{ form_row(form.lastName) }}
    {{ form_row(form.message) }}
    {{ form_row(form._token) }}
    <button type="submit">Submit</button>
</form>
