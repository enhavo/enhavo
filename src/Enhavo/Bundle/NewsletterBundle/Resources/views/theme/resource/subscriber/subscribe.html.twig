<div class='subscribe'>
    <form method="{{ form.vars.method }}" action="{{ form.vars.action }}" data-newsletter-subscribe-form>
        {{ form_widget(form._token) }}
        {{ form_widget(form.email) }}
        <button type="submit" id="addEmail" class="button">Add email</button>
    </form>
</div>
