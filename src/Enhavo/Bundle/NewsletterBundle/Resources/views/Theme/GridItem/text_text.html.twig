{% import _self as element %}

{% macro text(title, text, layout = 0) %}
    <td style="{% if layout == 0 %}width: 50%;{% endif %}{% if layout == 1 %}width: 33%;{% endif %}{% if layout == 2 %}width: 67%;{% endif %} padding: 10px;">
        {% if title %}
            <h2>{{ title }}</h2>
        {% endif %}
        {% if text %}
            <p>{{ text|raw }}</p>
        {% endif %}
    </td>
{% endmacro %}

<div style="padding-top: 10px; padding-bottom: 10px;">
    {% if data.title %}
        <h2>{{ data.title }}</h2>
    {% endif %}
    <table border="0">
        <tr>
            {{ element.text(data.titleLeft, data.textLeft, data.layout == 1 ? 1 : (data.layout == 2 ? 2 : 0)) }}
            {{ element.text(data.titleRight, data.textRight, data.layout == 1 ? 1 : (data.layout == 2 ? 2 : 0)) }}
        </tr>
    </table>
</div>