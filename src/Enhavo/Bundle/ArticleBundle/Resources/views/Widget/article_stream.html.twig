<div class="article-stream-widget">
    <h3>Categories</h3>
    <ul class="columns">
        {% for articles in ar %}
            <li><a href="{{ path('enhavo_article_theme_category', {slug: category.slug}) }}">{{ category }}</a></li>
        {% endfor %}
    </ul>
</div>