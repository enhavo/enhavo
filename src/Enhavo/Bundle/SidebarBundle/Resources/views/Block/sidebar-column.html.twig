{% if data.width == 'container' %}
    <div class="container">
{% endif %}
    <div class="row">
        <div class="col-md-8">
            {{ block_render(data.column) }}
        </div>
        <div class="col-md-4">
            {% if data.sidebar %}
                {{ block_render(data.sidebar.content) }}
            {% endif %}
        </div>
    </div>
{% if data.width == 'container' %}
    </div>
{% endif %}