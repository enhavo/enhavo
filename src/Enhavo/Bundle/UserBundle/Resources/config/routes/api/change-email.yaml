enhavo_user_api_change_email_request:
    options:
        expose: true
    path: /change-email/request
    methods: [GET, POST]
    defaults:
        _config: theme


enhavo_user_api_change_email_check:
    options:
        expose: true
    path: /change-email/check
    methods: [GET, POST]
    defaults:
        _config: theme

enhavo_user_api_change_email_confirm:
    options:
        expose: true
    path: /change-email/confirm/{token}
    methods: [GET, POST]
    defaults:
        _config: theme

enhavo_user_api_change_email_finish:
    options:
        expose: true
    path: /change-email/finish
    methods: [GET, POST]
    defaults:
        _config: theme