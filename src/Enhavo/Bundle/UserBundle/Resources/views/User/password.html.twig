{% extends 'EnhavoAppBundle::dialog.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}
{% block content %}
Passwort ändern:
    {{ form_start(form, { 'action': path('enhavo_user_user_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div>
        <input data-button type="submit" value="{{ 'change_password.submit'|trans }}" data-type="savePassword" id="changeUserPassword" />
    </div>
    {{ form_end(form) }}
{% endblock %}

{% block buttons %}
{% endblock %}