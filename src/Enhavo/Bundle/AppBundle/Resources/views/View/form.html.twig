{% for name,child in form %}
    {% if name != '_token' %}
        {{ form_row(child) }}
    {% endif %}
{% endfor %}

{% if form._token is defined %}
    {% if not form._token.rendered %}
        {{ form_row(form._token) }}
    {% endif %}
{% endif %}
