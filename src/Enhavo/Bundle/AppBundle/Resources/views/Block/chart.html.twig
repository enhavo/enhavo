{% trans_default_domain 'EnhavoAppBundle' %}

<div
    data-block
    data-block-app="{{ app }}"
    data-block-type="chart"
    data-block-chart-data="{{ data|json_encode|e }}"
    data-block-chart-options="{{ options|json_encode|e }}"
    data-block-chart-type="{{ type }}"
    data-block-chart-app="{{ chartApp }}"
    class="block-app block-chart"
>
    <div class="chart-container">
        <canvas></canvas>
    </div>
</div>