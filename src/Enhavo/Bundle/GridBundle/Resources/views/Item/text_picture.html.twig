<div class="row enhavo_content">
    <div class="enhavo_content_textpicture">
        {% if data.textLeft == 0 %}
        <div class="col-xs-6 left6">
            {% if data.file %}
                <img class="enhavo_content_img {% if data.frame %}enhavo_content_frame{% endif %}" src="{{ media_url(data.file,960) }}">
            {% endif %}
        </div>
        <div class="col-xs-6 right6">
            <div class="enhavo_content_text">
                {% if data.title %}
                    <h2>{{ data.title }}</h2>
                {% endif %}
                {% if data.text %}
                    {{ data.text|raw }}
                {% endif %}
            </div>
        </div>
        {% else %}
            <div class="col-xs-6 left6">
                <div class="enhavo_content_text">
                    {% if data.title %}
                        <h2>{{ data.title }}</h2>
                    {% endif %}
                    {% if data.text %}
                        {{ data.text|raw }}
                    {% endif %}
                </div>
            </div>
            <div class="col-xs-6 right6">
                {% if data.file %}
                    <img class="enhavo_content_img {% if data.frame %}enhavo_content_frame{% endif %}" src="{{ media_url(data.file,960) }}">
                {% endif %}
            </div>
        {% endif %}
    </div>
</div>