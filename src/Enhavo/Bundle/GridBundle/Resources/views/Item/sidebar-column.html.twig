{% if data.width == 'container' %}
    <div class="container">
{% endif %}
    <div class="row">
        <div class="col-md-8">
            {{ grid_render(data.column) }}
        </div>
        <div class="col-md-4">
            {{ grid_render(data.sidebar.grid) }}
        </div>
    </div>
{% if data.width == 'container' %}
    </div>
{% endif %}