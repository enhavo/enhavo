<div class="enhavo_grid">
    <div class="container enhavo_grid_text">
        {% if data.title %}
            <h2 class="title">{{ data.title|raw }}</h2>
        {% endif %}
        {% if data.text %}
            {{ data.text|raw }}
        {% endif %}
    </div>
</div>
