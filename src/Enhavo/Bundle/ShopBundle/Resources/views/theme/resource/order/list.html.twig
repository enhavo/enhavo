{% extends template('theme/base.html.twig') %}

{% block main %}
    {% for order in orders %}
        <div>
            <a href="{{ path('enhavo_shop_theme_user_order_detail', {id: order.id}) }}">{{ order.id }}</a>
        </div>
    {% endfor %}
{% endblock %}