{% extends template('theme/base.html.twig') %}

{% block main %}
    <h1>Select Payment</h1>
    <div>
        <form action="{{ path('sylius_shop_checkout_select_payment') }}" method="POST" novalidate>
            {{ form_row(form.payments) }}
            {{ form_row(form._token) }}
            <button type="submit">Senden</button>
        </form>
    </div>
{% endblock %}
