pictureWidth
~~~~~~~~~~~~

**type**: `integer`

Define the width of the thumbnail in pixels. The default value is ``60``.
If the column the widget is in is smaller than this value, the thumbnail will be resized to fit the column.

.. code-block:: yaml

    columns:
        myWidget:
            pictureWidth: 60
            # ... further option