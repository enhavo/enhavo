.. _display:

display
~~~~~~~

**type**: `boolean`

If ``false`` the button won't be rendered. The default value is ``true``

.. code-block:: yaml

    buttons:
        myButton:
            display: false
            # ... further option