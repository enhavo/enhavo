admin:
    prefix: /admin
    resource: routes/admin/*

app:
    resource: ../src/App/Controller
    type: annotation

endpoint:
    resource: ../src/App/Endpoint
    type: endpoint

app_controller:
    resource: ../src/App/Controller
    type: annotation
