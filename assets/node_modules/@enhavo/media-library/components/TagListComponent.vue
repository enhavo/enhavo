<template>
    <ul>
        <tag-component
            v-for="tag of categories"
            :tag="tag"
            :parents="parents"
            v-bind:key="tag.MediaLibraryId"
        ></tag-component>
    </ul>
</template>

<script lang="ts">
    import { Vue, Component, Prop } from "vue-property-decorator";
    import {Tag} from "../Data";

    @Component({})
    export default class TagListComponent extends Vue
    {
        name: 'tag-list';

        @Prop()
        categories: Array<Tag>;

        @Prop()
        parents: Array<number>;
    }
</script>
