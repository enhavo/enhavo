import * as jQuery from "jquery";
import AbstractAction from "@enhavo/app/action/model/AbstractAction";

export default class EventAction extends AbstractAction
{
    public event: string;

    execute(): void
    {
        jQuery(document).trigger(this.event);
    }
}