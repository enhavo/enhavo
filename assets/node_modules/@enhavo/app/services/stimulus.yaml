compiler_pass:
    stimulus:
        path: '../compiler-pass/stimulus-register.js'
        service: '@hotwired/stimulus/Application'
        tag: 'stimulus.controller'

services:
    '@hotwired/stimulus/Application':
        chunk: 'stimulus'
        from: '@hotwired/stimulus'
        import: Application
