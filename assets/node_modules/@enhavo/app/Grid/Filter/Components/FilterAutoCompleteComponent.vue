<template>
    <div class="view-table-filter-search">
        <v-select :options="data.choices" @search="fetchOptions" :placeholder="data.label" v-model="data.selected" @input="change"></v-select>
    </div>
</template>

<script lang="ts">
    import { Vue, Component, Prop } from "vue-property-decorator";
<<<<<<< HEAD:assets/node_modules/@enhavo/app/Grid/Filter/Components/FilterAutoCompleteComponent.vue
    import AbstractFilter from "@enhavo/app/Grid/Filter/Model/AbstractFilter";
=======
    import AutoCompleteEntityFilter from "@enhavo/app/grid/filter/model/AutoCompleteEntityFilter";
>>>>>>> d1fcf07da ([AppBundle] Added initial values for filters (#1275)):assets/node_modules/@enhavo/app/grid/filter/components/FilterAutoCompleteComponent.vue
    import axios from 'axios';
    import * as URI from 'urijs';

    @Component
    export default class FilterAutoCompleteComponent extends Vue
    {
        name: string = 'filter-autocomplete';

        @Prop()
        data: AutoCompleteEntityFilter;

        change(value: any) {
            if(value == null) {
                this.data.value = null;
                return;
            }
            this.data.value = value.code;
        }

        fetchOptions(search: string, loading: (value: boolean) => void)
        {
            if(search.length < this.data.minimumInputLength) {
                return;
            }
            loading(true);

            let uri = new URI(this.data.url);
            uri.addQuery('q', search);
            uri.addQuery('page', 1);
            let uriString = uri + '';

            axios
                .get(uriString)
                .then((data) => {
                    this.data.choices = [];
                    for(let result of data.data.results) {
                        this.data.choices.push({
                            label: result.text,
                            code: result.id
                        })
                    }
                    loading(false);
                });
        }
    }
</script>






